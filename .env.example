# MPG アクセストークン（.env にコピーして使用。.env はコミット禁止）
#
# 取得手順:
#   1. ブラウザで https://mpg.football/ にログイン
#   2. DevTools を開く（F12）→ Console タブ
#   3. 次を実行してトークンを取り出す:
#        Object.entries(localStorage)
#          .filter(([k]) => k.includes('auth0'))
#          .map(([,v]) => JSON.parse(v)?.body?.access_token)
#          .filter(Boolean)[0]
#   4. 出力された文字列（eyJ... で始まる）を下に貼る
#
# トークンは数時間で失効する。失効したら取り直すこと。
MPG_TOKEN=

# 任意: client-version ヘッダを上書きする場合のみ設定（既定は 13.2.0）
# MPG の更新で 400 が出るようになったら、最新のフロントバージョンを設定する
# MPG_CLIENT_VERSION=

# 任意: 対象ディビジョンを明示指定（カンマ区切り可）
# 未設定なら dashboard とリーグ情報から全シーズン分を自動収集する
# 例: MPG_DIVISION_ID=mpg_division_PHDHUA3Z_7_1,mpg_division_PHDHUA3Z_8_1
# MPG_DIVISION_ID=
